{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tokenpak.ai/schemas/manifests/plugin-v1.json",
  "title": "TIP-1.0 Plugin Manifest",
  "description": "Declares an optional TokenPak plugin. Phase 1 stub.",
  "type": "object",
  "required": ["tip_version", "id", "name", "version", "kind", "capabilities"],
  "properties": {
    "tip_version": { "type": "string", "pattern": "^TIP-\\d+\\.\\d+$" },
    "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
    "name": { "type": "string" },
    "version": { "type": "string" },
    "kind": { "const": "plugin" },
    "capabilities": { "type": "array", "items": { "type": "string" } },
    "compatibility": { "$ref": "../tip/compatibility.schema.json" },
    "trust": { "type": "object" },
    "extensions": { "type": "object" }
  }
}
